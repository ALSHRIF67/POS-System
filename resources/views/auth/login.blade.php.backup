<x-guest-layout>
    <!-- Font and custom styles -->
    @push('styles')
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Cairo', sans-serif;
            background: #f3f4f6;
        }
        
        /* Touch-friendly button styles */
        .touch-button {
            min-height: 60px;
            cursor: pointer;
            -webkit-tap-highlight-color: transparent;
            transition: transform 0.1s ease;
        }
        
        .touch-button:active {
            transform: scale(0.98);
        }
        
        /* Custom focus styles */
        .focus-ring-custom:focus {
            outline: none;
            ring: 2px solid #6C63FF;
            ring-offset: 2px;
        }
    </style>
    @endpush

    <!-- Session Status (styled as alert) -->
    <div class="mb-4 w-full max-w-md mx-auto">
        <x-auth-session-status class="text-sm font-medium text-green-600 dark:text-green-400" :status="session('status')" />
    </div>

    <!-- Card container with POS style -->
    <div class="w-full max-w-md mx-auto overflow-hidden rounded-2xl bg-white shadow-lg dark:bg-gray-800">
        <div class="p-6 sm:p-8">
            <form method="POST" action="{{ route('login') }}">
                @csrf

                <!-- Email Address -->
                <div>
                    <x-input-label for="email" :value="__('Email')" class="text-gray-700 dark:text-gray-300 font-semibold" />
                    <x-text-input
                        id="email"
                        class="mt-1 block w-full rounded-xl border-2 border-gray-200 bg-white py-4 px-4 text-gray-900 placeholder-gray-400 focus:border-[#6C63FF] focus:ring-0 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-300 dark:focus:border-[#6C63FF] sm:text-sm"
                        type="email"
                        name="email"
                        :value="old('email')"
                        required
                        autofocus
                        autocomplete="username"
                        placeholder="your@email.com"
                    />
                    <x-input-error :messages="$errors->get('email')" class="mt-2" />
                </div>

                <!-- Password -->
                <div class="mt-4">
                    <x-input-label for="password" :value="__('Password')" class="text-gray-700 dark:text-gray-300 font-semibold" />
                    <x-text-input
                        id="password"
                        class="mt-1 block w-full rounded-xl border-2 border-gray-200 bg-white py-4 px-4 text-gray-900 placeholder-gray-400 focus:border-[#6C63FF] focus:ring-0 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-300 dark:focus:border-[#6C63FF] sm:text-sm"
                        type="password"
                        name="password"
                        required
                        autocomplete="current-password"
                        placeholder="••••••••"
                    />
                    <x-input-error :messages="$errors->get('password')" class="mt-2" />
                </div>

                <!-- Remember Me -->
                <div class="mt-4">
                    <label for="remember_me" class="inline-flex items-center">
                        <input
                            id="remember_me"
                            type="checkbox"
                            class="rounded border-gray-300 text-[#6C63FF] shadow-sm focus:ring-[#6C63FF] dark:border-gray-700 dark:bg-gray-900 dark:focus:ring-[#6C63FF] dark:focus:ring-offset-gray-800"
                            name="remember"
                        >
                        <span class="ms-2 text-sm text-gray-600 dark:text-gray-400">
                            {{ __('Remember me') }}
                        </span>
                    </label>
                </div>

                <!-- Forgot Password & Login Button -->
                <div class="mt-6 flex flex-col space-y-4 sm:flex-row sm:items-center sm:justify-between sm:space-y-0">
                    @if (Route::has('password.request'))
                        <a
                            href="{{ route('password.request') }}"
                            class="text-center text-sm text-gray-600 underline hover:text-[#6C63FF] focus:outline-none focus:ring-2 focus:ring-[#6C63FF] focus:ring-offset-2 dark:text-gray-400 dark:hover:text-[#6C63FF] dark:focus:ring-offset-gray-800 sm:text-left"
                        >
                            {{ __('Forgot your password?') }}
                        </a>
                    @endif

                    <x-primary-button class="w-full justify-center bg-gradient-to-l from-[#6C63FF] to-[#FF6B6B] px-8 py-4 text-white rounded-xl font-bold shadow-md hover:opacity-90 focus:outline-none focus:ring-2 focus:ring-[#6C63FF] focus:ring-offset-2 dark:focus:ring-offset-gray-800 touch-button sm:w-auto">
                        {{ __('Log in') }}
                    </x-primary-button>
                </div>
            </form>

            <!-- Register Link -->
            <div class="mt-6 text-center text-sm text-gray-600 dark:text-gray-400">
                {{ __("Don't have an account?") }}
                <a
                    href="{{ route('register') }}"
                    class="font-medium text-[#6C63FF] underline hover:text-[#5a52d5] focus:outline-none focus:ring-2 focus:ring-[#6C63FF] focus:ring-offset-2 dark:focus:ring-offset-gray-800"
                >
                    {{ __('Register') }}
                </a>
            </div>
        </div>
    </div>
</x-guest-layout>
