<x-templte/>

<main class="main-content min-h-screen bg-[#f8fafc] p-4">
    <div class="max-w-4xl mx-auto">
        <!-- Header with back button -->
        <div class="flex items-center justify-between mb-6">
            <h1 class="text-2xl font-bold text-gray-800">
                <i class="fas fa-receipt ml-2 text-[#6C63FF]"></i>
                تفاصيل الطلب #{{ $order->order_number }}
            </h1>
            <a href="{{ route('orders.index') }}" class="btn-secondary flex items-center gap-2 px-4 py-2 bg-gray-100 rounded-xl hover:bg-gray-200 transition">
                <i class="fas fa-arrow-right"></i>
                العودة للقائمة
            </a>
        </div>

        <!-- Order Info Card -->
        <div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-6 mb-6">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                <div>
                    <p class="text-sm text-gray-500 mb-1">رقم الطلب</p>
                    <p class="font-bold text-lg">{{ $order->order_number }}</p>
                </div>
                <div>
                    <p class="text-sm text-gray-500 mb-1">التاريخ</p>
                    <p class="font-bold">{{ \Carbon\Carbon::parse($order->created_at)->format('Y-m-d h:i A') }}</p>
                </div>
                <div>
                    <p class="text-sm text-gray-500 mb-1">طريقة الدفع</p>
                    <p class="font-bold">
                        @if($order->payment_method == 'cash') نقدي
                        @elseif($order->payment_method == 'card') بطاقة
                        @elseif($order->payment_method == 'wallet') محفظة
                        @else {{ $order->payment_method }}
                        @endif
                    </p>
                </div>
                <div>
                    <p class="text-sm text-gray-500 mb-1">الحالة</p>
                    <span class="px-3 py-1.5 text-xs rounded-xl font-medium
                        @if($order->status == 'completed') bg-green-100 text-green-600
                        @elseif($order->status == 'pending') bg-yellow-100 text-yellow-600
                        @else bg-red-100 text-red-600
                        @endif">
                        @if($order->status == 'completed') مكتمل
                        @elseif($order->status == 'pending') قيد الانتظار
                        @else ملغي
                        @endif
                    </span>
                </div>
            </div>
            @if($order->notes)
            <div class="mt-4 p-3 bg-gray-50 rounded-xl">
                <p class="text-sm text-gray-500 mb-1">ملاحظات:</p>
                <p class="text-gray-700">{{ $order->notes }}</p>
            </div>
            @endif
        </div>

        <!-- Order Items Table -->
        <div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden">
            <div class="p-4 border-b border-gray-100 bg-gray-50">
                <h2 class="font-bold text-gray-700">الأصناف</h2>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full text-right">
                    <thead class="bg-gray-100">
                        <tr>
                            <th class="px-6 py-3 text-sm font-medium text-gray-500">المنتج</th>
                            <th class="px-6 py-3 text-sm font-medium text-gray-500">السعر</th>
                            <th class="px-6 py-3 text-sm font-medium text-gray-500">الكمية</th>
                            <th class="px-6 py-3 text-sm font-medium text-gray-500">الإجمالي</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        @foreach($order->items as $item)
                        <tr>
                            <td class="px-6 py-4">{{ $item->product_name }}</td>
                            <td class="px-6 py-4">{{ number_format($item->price, 2) }} ج.م</td>
                            <td class="px-6 py-4">{{ $item->quantity }}</td>
                            <td class="px-6 py-4 font-bold text-[#6C63FF]">{{ number_format($item->total, 2) }} ج.م</td>
                        </tr>
                        @endforeach
                    </tbody>
                </table>
            </div>
            <!-- Totals -->
            <div class="p-4 border-t border-gray-100 bg-gray-50">
                <div class="flex justify-between items-center mb-2">
                    <span class="text-gray-600">المجموع الفرعي:</span>
                    <span class="font-bold">{{ number_format($order->subtotal, 2) }} ج.م</span>
                </div>
                @if($order->tax > 0)
                <div class="flex justify-between items-center mb-2">
                    <span class="text-gray-600">الضريبة:</span>
                    <span class="font-bold">{{ number_format($order->tax, 2) }} ج.م</span>
                </div>
                @endif
                @if($order->discount > 0)
                <div class="flex justify-between items-center mb-2">
                    <span class="text-gray-600">الخصم:</span>
                    <span class="font-bold text-red-500">{{ number_format($order->discount, 2) }} ج.م</span>
                </div>
                @endif
                <div class="flex justify-between items-center text-lg pt-2 border-t border-gray-200">
                    <span class="font-bold text-gray-700">الإجمالي:</span>
                    <span class="font-bold text-2xl text-[#6C63FF]">{{ number_format($order->total, 2) }} ج.م</span>
                </div>
            </div>
        </div>

       
            <a href="{{ route('orders.index') }}" class="btn-primary bg-[#6C63FF] text-white px-6 py-3 rounded-xl hover:bg-[#5a52d5] transition flex items-center gap-2">
                <i class="fas fa-check"></i>
                تم
            </a>
        </div>
    </div>
</main>

<style>
    @media print {
        .main-content { background: white; padding: 20px; }
        .btn-secondary, .btn-primary, header, footer, .sidebar { display: none; }
    }
</style>
